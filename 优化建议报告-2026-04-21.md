# try7z 项目优化建议报告

> 生成日期：2026-04-21
> 扫描范围：完整代码库

---

## 执行摘要

本次扫描在 try7z 项目中识别出 **47 项** 优化机会，分布在 10 个类别中。截至目前，**34 项** 已修复，**13 项** 待处理。项目整体结构良好、文档完善、测试覆盖较全面，但在安全性、错误处理、性能和代码复杂度方面仍存在改进空间。

---

## 1. 安全性

- [ ] **CRITICAL-1**: 明文密码存储 - `try7z/password_manager.py:52-130`
  - 密码以完全未加密的 JSON 格式存储，任何有文件读取权限的进程都能窃取
  - 建议：至少实现 base64 混淆，或集成 `keyring` 库使用系统密钥环

- [ ] **HIGH-1**: `get_7z_version()` 静默吞掉所有错误 - `try7z/extractor.py:108-117`
  - 裸 `except Exception` 静默捕获所有错误，包括权限拒绝等安全问题
  - 建议：捕获具体异常类型（`subprocess.SubprocessError`, `OSError`, `FileNotFoundError`）

- [x] **HIGH-2**: 密码内容无输入验证 - `try7z/password_manager.py:132-158` ✅ **已修复**
  - ~~`add_password()` 接受任意字符串，空字符串、纯空白或超长字符串可能引发问题~~
  - ~~建议：添加验证 - 拒绝空字符串、去除空白、设置合理最大长度~~
  - **修复内容**：
    - 在 `try7z/utils.py` 添加 `PasswordValidator` 抽象基类和 `BasicPasswordValidator` 实现
    - `BasicPasswordValidator` 验证：非空、非纯空白、不超过 1000 字符
    - `PasswordManager.add_password()` 默认使用 `BasicPasswordValidator`，支持自定义验证器
    - 新增 `PasswordValidationError` 异常，CLI 层捕获并显示警告
    - 新增测试覆盖空字符串、纯空白、超长密码等边界情况

- [ ] **MEDIUM-1**: 密码在进程列表中可见 - `try7z/extractor.py:386-389`
  - 密码直接作为命令行参数传递（`-p{password}`），`ps` 或 Process Explorer 可见
  - 建议：使用 7-Zip 的环境变量或 stdin 传递密码能力，或至少文档说明此限制

- [ ] **MEDIUM-2**: 密码文件无权限检查 - `try7z/password_manager.py:100-103`
  - 密码 JSON 文件以默认权限创建，Unix 系统上其他用户可能可读
  - 建议：创建后设置严格文件权限（`0o600`），特别是 Linux/macOS

---

## 2. 错误处理

- [x] **CRITICAL-2**: `_load_passwords()` 静默损坏数据 - `try7z/password_manager.py:108-121` ✅ **已修复**
  - ~~密码文件 JSON 无效时静默重置为空列表，**丢失所有用户数据**且无警告~~
  - ~~建议：区分"文件不存在"（正常开始）和"文件存在但损坏"（警告用户、备份损坏文件后重置）~~
  - **修复内容**：
    - 文件不存在时正常开始（空列表）
    - 文件损坏时（JSON 无效、缺少 `passwords` 键、非 JSON 对象）抛出 `PasswordManagerError`
    - 损坏文件自动备份为 `.json.bak`
    - 错误消息包含备份路径和操作说明
    - 新增 3 个测试用例覆盖损坏文件场景

- [ ] **HIGH-3**: `cmd_extract()` 输出目录检查存在竞态条件 - `try7z/main.py:438-453`
  - `output_dir.exists()` 和 `shutil.rmtree()` 之间非原子，目录可能在检查和删除之间被创建/修改
  - 建议：直接用 `shutil.rmtree()` 并配合适当的错误处理，或用 try/except 包裹目录操作

- [ ] **HIGH-4**: `remove_by_index()` 错误消息误导 - `try7z/password_manager.py:276-277`
  - 错误显示 "Index {index + 1} out of range" 但参数是 0-based，用户传入 5 却显示 "Index 6"
  - 建议：使错误消息更清晰：`"Index {index} (1-based: {index + 1}) out of range"`

- [ ] **HIGH-5**: `cmd_edit_passwords()` 捕获裸 `Exception` - `try7z/main.py:370-380`
  - 泛型捕获所有异常，调试困难
  - 建议：捕获具体异常：`OSError`, `subprocess.CalledProcessError`, `FileNotFoundError`

- [ ] **MEDIUM-3**: `_extract_with_password()` 密码检测过于宽泛 - `try7z/extractor.py:404-407`
  - 检查 `'password' in error_output` 过于宽泛，任何包含 "password" 的输出都可能被误判
  - 建议：使用 7-Zip 文档化的更具体的错误代码和消息模式

- [ ] **MEDIUM-4**: `cmd_clear_passwords()` 在确认后创建管理器 - `try7z/main.py:292-302`
  - 确认提示在实例化 `PasswordManager` 之前显示，如果用户取消，数据目录仍可能被创建
  - 建议：在显示提示前实例化 `PasswordManager`，或仅在确认后才创建目录

- [ ] **LOW-1**: `get_7z_path()` 非 Windows 平台无验证 - `try7z/extractor.py:82-88`
  - Linux/macOS 路径直接返回而不验证二进制是否存在，导致后续 `FileNotFoundError`
  - 建议：检查二进制是否存在，不存在时立即抛出描述性错误

---

## 3. 性能

- [ ] **HIGH-6**: 两阶段提取导致重复解压 - `try7z/extractor.py:285-334`
  - `show_progress=True` 时解压两次：先找密码，再显示进度。大文件耗时翻倍
  - 建议：在密码发现阶段缓存提取的文件，或使用单次提取同时捕获密码验证和进度信息

- [ ] **HIGH-7**: `_get_archive_file_count()` 额外启动 7-Zip 进程 - `try7z/extractor.py:182-212`
  - 每次带进度提取时额外启动一个 7-Zip 进程仅用于计数文件
  - 建议：跳过文件计数（进度条无此信息也能工作），或从提取输出本身解析文件数

- [ ] **MEDIUM-5**: `_extract_with_progress()` 逐字节读取 - `try7z/extractor.py:467-513`
  - 逐字节读取 stdout（`process.stdout.read(1)`），大输出时效率极低
  - 建议：以更大块读取（如 4096 字节）并内部缓冲，或使用 `readline()` 行缓冲读取

- [x] **MEDIUM-6**: `password_manager.py` 每次更改都重写整个文件 - `try7z/password_manager.py:123-130` ✅ **已修复**
  - ~~每次 `add_password()`、`remove_password()` 等都触发完整文件重写，批量操作时浪费~~
  - ~~建议：添加上下文管理器或批处理模式延迟写入~~
  - **修复内容**：
    - 添加 `auto_save: bool = True` 构造函数参数，默认保持向后兼容
    - 新增 `_dirty` 标记，仅在有修改时写入
    - 新增 `save()` 公共方法，支持手动触发保存
    - 实现 `__enter__`/`__exit__` 上下文管理器：进入时禁用 auto_save，退出时自动保存（异常时不保存）
    - 批量添加 100 个密码从 100 次写入降至 **1 次写入**
    - 新增 7 个测试用例覆盖 batch 模式、context manager、异常安全

- [x] **LOW-2**: `cmd_remove_password()` 手动去重索引 - `try7z/main.py:166-171` ✅ **已修复**
  - ~~使用 set 和 list 手动去重，不如 `dict.fromkeys()` 保持顺序高效~~
  - ~~建议：替换为 `unique_indices = list(dict.fromkeys(args.index))`~~
  - **修复内容**：
    - `RemoveByValueStrategy` 和 `RemoveByIndexStrategy` 均使用 `list(dict.fromkeys())` 去重
    - 保持原始顺序，O(n) 时间复杂度，比 set+list 更简洁

---

## 4. 代码质量 / 反模式

- [x] **HIGH-8**: `cmd_remove_password()` 复杂度过高 - `try7z/main.py:109-215` ✅ **已修复**
  - ~~约 100 行，嵌套条件，两个非常相似的代码路径（按索引删除 vs 按值删除），重复的报告逻辑~~
  - ~~建议：为每种删除模式提取辅助方法，或使用策略模式~~
  - **修复内容**：
    - 定义 `RemovalStrategy` Protocol 接口
    - 实现 `RemoveByValueStrategy`：封装去重（`dict.fromkeys()`）和按值删除逻辑
    - 实现 `RemoveByIndexStrategy`：封装去重、1-based 转 0-based、降序排序逻辑
    - 提取 `_report_removal_result()` 统一报告成功/失败消息
    - `cmd_remove_password()` 从 ~109 行缩减至 ~25 行：验证参数 → 选择策略 → 执行 → 报告
    - 新增 9 个单元测试覆盖策略类和报告函数

- [x] **HIGH-9**: `try_extract()` 两个几乎相同的代码路径 - `try7z/extractor.py:284-347` ✅ **已修复**
  - ~~`show_progress=True` 和 `show_progress=False` 分支重复核心密码尝试循环，仅有细微差异~~
  - ~~建议：将密码尝试循环提取为独立方法，然后用进度显示逻辑包装~~
  - **修复内容**：
    - 提取 `_try_passwords()` 方法，封装密码尝试循环（含 `show_password_progress` 支持）
    - 返回 `(success, used_password, attempts_count)` 三元组，支持进度消息显示尝试次数
    - `try_extract()` 从 ~65 行缩减至 ~25 行：`show_progress` 分支调用 `_try_passwords()` → 成功后重新提取带进度
    - 新增 6 个单元测试覆盖 `_try_passwords()` 的成功、失败、进度显示、异常传播等场景

- [x] **MEDIUM-7**: `extract_with_passwords()` 冗余检查 - `try7z/extractor.py:605-606` ✅ **已修复**
  - ~~`if not success and passwords:` 冗余，因为 `try_extract()` 已处理空密码列表~~
  - ~~建议：简化为 `if not success:` 或完全移除检查~~
  - **修复内容**：移除 `and passwords` 条件，简化为 `if not success:`

- [x] **MEDIUM-8**: `cmd_add_password()` 退出码逻辑反直觉 - `try7z/main.py:106` ✅ **已修复**
  - ~~所有密码都是重复时返回 1，但操作本身并未"失败"，只是无效果。UNIX 惯例应返回 0~~
  - ~~建议：始终返回 0 表示成功完成，或清晰文档说明退出码行为~~
  - **修复内容**：`cmd_add_password()` 始终返回 `0`，更新 `test_add_all_duplicates` 断言

- [x] **LOW-3**: 方法内导入 `shutil` - `try7z/extractor.py:320` ✅ **已修复**
  - ~~`shutil` 在 `try_extract()` 内部导入而非模块顶部~~
  - ~~建议：移至顶层导入~~
  - **修复内容**：将 `import shutil` 移至模块顶部导入区

- [x] **LOW-4**: 方法内导入 `tqdm` - `try7z/extractor.py:440` ✅ **已修复**
  - ~~`tqdm` 在 `_extract_with_progress()` 内部导入，与项目风格不一致~~
  - ~~建议：移至顶层，或文档说明延迟加载理由~~
  - **修复内容**：将 `from tqdm import tqdm` 移至模块顶部导入区

---

## 5. 可维护性 / 架构

- [x] **HIGH-10**: CLI 命令与 PasswordManager 紧耦合 - `try7z/main.py:61-489` ✅ **已修复**
  - ~~每个命令处理程序都用相同默认逻辑实例化 `PasswordManager`，更换存储后端需修改每个函数~~
  - ~~建议：在 `main()` 级别使用依赖注入，或创建上下文管理器/工厂模式~~
  - **修复内容**：
    - 添加 `_get_password_manager()` 工厂函数，集中 `PasswordManager` 实例化
    - 所有命令处理函数统一通过工厂函数获取实例，不再直接调用 `PasswordManager()`
    - 更换存储后端时只需修改工厂函数一处，无需修改每个命令处理函数
    - 保持向后兼容：测试仍可通过 `manager` 参数注入 mock

- [x] **HIGH-11**: `cmd_extract()` 混合多种职责 - `try7z/main.py:383-489` ✅ **已修复**
  - ~~一个 100+ 行函数处理参数解析、路径验证、用户确认提示、目录清理、密码管理、提取编排~~
  - ~~建议：将路径准备、确认逻辑和输出清理提取为独立辅助函数~~
  - **修复内容**：
    - 提取 `_build_password_list()`：构建密码列表（支持优先密码前置）
    - 提取 `_resolve_output_dir()`：解析输出目录路径（支持单/多 archive 模式）
    - 提取 `_handle_existing_output()`：处理已存在输出的确认/删除逻辑
    - `cmd_extract()` 从 100+ 行缩减至约 40 行，职责清晰：构建密码 → 循环处理 → 汇总统计
    - `_extract_single()` 进一步简化，专注提取执行和结果处理

- [x] **MEDIUM-9**: `PasswordManager` 缺少接口/抽象基类 - `try7z/password_manager.py:58-281` ✅ **已修复**
  - ~~无抽象层意味着提取器与 JSON 文件存储紧耦合~~
  - ~~建议：定义 `PasswordStore` 协议或 ABC，`PasswordManager` 实现它~~
  - **修复内容**：
    - 添加 `@runtime_checkable` 的 `PasswordStore` Protocol，定义所有公共方法签名
    - `PasswordManager` 隐式实现该协议，获得结构子类型检查支持
    - 新增 doctest 示例展示 Protocol 用法

- [x] **MEDIUM-10**: `get_user_data_dir()` 文档字符串误导 - `try7z/utils.py:139-172` ✅ **已修复**
  - ~~文档说"此函数不会自动创建目录"，但 `PasswordManager.__init__()` 会创建。注释令人困惑~~
  - ~~建议：更新文档字符串准确描述行为~~
  - **修复内容**：
    - 更新 Note 部分，明确说明"此函数返回路径但不创建目录，调用组件（如 PasswordManager）负责在需要时创建"
    - 消除文档与实际行为之间的歧义

- [x] **LOW-5**: `__init__.py` 文档字符串引用不存在的 API - `try7z/__init__.py:27-28` ✅ **已修复**
  - ~~示例显示 `extractor.try_extract(passwords=...)` 但实际方法是 `extract_with_passwords()` 或参数顺序不同的 `try_extract()`~~
  - ~~建议：更新示例以匹配实际 API~~
  - **修复内容**：
    - 更新 `__init__.py` docstring 示例，正确使用 `Extractor("archive.7z")` 和 `extractor.try_extract(passwords=pm.get_passwords())`
    - 示例与实际 API 签名一致

---

## 6. 测试

- [x] **HIGH-12**: 无 `get_7z_path()` 非 Windows 平台测试 - `tests/test_extractor.py` ✅ **已修复**
  - ~~`get_7z_path()` 有平台特定逻辑但测试仅在当前平台运行，未模拟 `platform.system()` 或 `platform.machine()`~~
  - **修复内容**：添加 `TestGet7zPath` 测试类，模拟 `platform.system()` 和 `platform.machine()`，覆盖 Windows (AMD64/x86_64)、Linux (amd64/x86_64)、macOS 及不支持的系统/架构

- [x] **HIGH-13**: 无 `_extract_with_progress()` 测试 - `tests/test_extractor.py` ✅ **已修复**
  - ~~进度条提取路径（`_extract_with_progress()`）零测试覆盖，是代码库中最复杂的方法之一~~
  - **修复内容**：添加 `TestExtractWithProgress` 测试类，使用 `unittest.mock` 模拟 `subprocess.Popen` 和逐字节 stdout 读取，覆盖成功提取、密码错误、提取失败三种场景

- [x] **HIGH-14**: 密码管理器边界情况测试缺失 - `tests/test_password_manager.py` ✅ **已修复**
  - ~~缺少：空字符串密码、超长密码、并发访问、损坏的 JSON 文件、文件权限错误~~
  - **修复内容**：添加 `TestEdgeCases` 测试类，覆盖空字符串密码、纯空白密码、10000字符超长密码、多实例并发写入、文件加载权限错误、文件保存权限错误

- [x] **MEDIUM-11**: CLI 测试未直接测试 `main()` 函数 - `tests/test_cli.py` ✅ **已修复**
  - ~~测试直接调用命令处理程序并传入 `argparse.Namespace`，但未测试通过 `main()` 的实际参数解析流程~~
  - **修复内容**：在 `TestMain` 中新增 `test_main_list_command`、`test_main_path_command`、`test_main_remove_command`，直接调用 `main()` 并修补 `sys.argv`

- [x] **MEDIUM-12**: `cmd_edit_passwords()` macOS/Linux 测试缺失 - `tests/test_cli.py:360-384` ✅ **已修复**
  - ~~`test_edit_opens_file` 仅修补 `os.startfile`（Windows），macOS/Linux 分支（`open`, `xdg-open`）未测试~~
  - **修复内容**：新增 `test_edit_opens_file_macos` 和 `test_edit_opens_file_linux`，分别模拟 `sys.platform` 和 `os.name` 验证对应分支

- [x] **MEDIUM-13**: 无 `_get_archive_file_count()` 测试 - `tests/test_extractor.py` ✅ **已修复**
  - ~~此方法解析 7-Zip 列表输出但无测试验证正确解析或错误处理~~
  - **修复内容**：添加 `TestGetArchiveFileCount` 测试类，覆盖真实归档调用、Files 行解析成功、无 Files 行、子进程超时、非零返回码

- [x] **LOW-6**: 测试夹具跨文件重复 - `tests/test_cli.py:24-72`, `tests/test_extractor.py:13-87` ✅ **已修复**
  - ~~`temp_dir`, `seven_zip`, `plain_7z_archive`, `encrypted_7z_archive` 夹具在多个测试文件中重复~~
  - **修复内容**：创建 `tests/conftest.py`，将 `temp_dir`、`seven_zip`、`plain_7z_archive`、`encrypted_7z_archive` 提取为共享 pytest 夹具

---

## 7. 文档

- [x] **MEDIUM-14**: README 缺少 `extract -f` 标志说明 - `README.md:95-106` ✅ **已修复**
  - ~~`extract` 命令的 `-f/--force` 标志在 README 使用示例中未文档化，但代码中存在~~
  - **修复内容**：在 Archive Extraction 部分添加 `-f` 标志使用示例

- [x] **MEDIUM-15**: `docs/modules.rst` 使用 `:undoc-members:` 与指南冲突 - `docs/modules.rst:13-44` ✅ **已修复**
  - ~~AGENTS.md 要求"所有公共模块、类和函数必须有 Google 风格文档字符串"且 `napoleon_include_private_with_doc = False`，但 `:undoc-members:` 仍会显示未文档成员~~
  - **修复内容**：从全部 5 个 automodule 指令中移除 `:undoc-members:`；已验证所有公共成员均有 Google 风格文档字符串

- [x] **LOW-7**: `docs/conf.py` 硬编码版本 - `docs/conf.py:17-18` ✅ **已修复**
  - ~~版本 `0.5.0` 硬编码而非从包导入~~
  - **修复内容**：动态导入 `from try7z import __version__`，失败时回退到 `'0.5.1'`
  - **附加修复**：`html_static_path = ['_static']` 改为 `html_static_path = []`，消除 Sphinx 构建警告

- [x] **LOW-8**: 缺少变更日志/发布说明 ✅ **已确认无需修复**
  - `CHANGELOG.md` 已存在且由 `cz` (Commitizen) 工具维护，采用其标准格式

---

## 8. 用户体验

- [x] **HIGH-15**: `cmd_extract()` 使用 `-f` 时无警告直接覆盖 - `try7z/main.py:439-453` ✅ **已修复**
  - ~~使用 `-f` 时通过 `shutil.rmtree()` 静默删除现有输出，破坏性且不可逆~~
  - ~~建议：添加备份选项，或至少在删除前显示将被删除的内容~~
  - **修复内容**：
    - `_extract_single()` 在强制覆盖前输出警告：`"Warning: Output directory 'xxx' already exists and will be overwritten."`
    - 区分文件和目录，显示准确的类型名称

- [x] **MEDIUM-16**: 密码尝试阶段无进度指示 - `try7z/extractor.py:284-347` ✅ **已修复**
  - ~~尝试多个密码且无 `show_progress` 时无反馈，用户可能认为程序卡死~~
  - ~~建议：添加简单旋转器或定期状态更新~~
  - **修复内容**：
    - `try_extract()` 新增 `show_password_progress: bool = False` 参数
    - 启用时在同一行刷新显示 `"Trying password X/N..."`
    - 找到密码后显示 `"Found after X trie(s)!"`
    - 未找到时自动换行，保持终端整洁

- [x] **MEDIUM-17**: `cmd_add_password()` 不显示具体添加了哪些密码 - `try7z/main.py:93-104` ✅ **已修复**
  - ~~仅显示计数，不显示具体密码。用户可能不确定~~
  - ~~建议：列出已添加的密码（长列表时截断）~~
  - **修复内容**：
    - 成功添加后列出具体密码，格式：`Added N password(s): 'pwd1', 'pwd2'...`
    - 超过 5 个密码时显示 `"and X more"`
    - 同时显示无效/重复密码的跳过警告

- [x] **LOW-9**: `cmd_list_passwords()` 不处理长密码 - `try7z/main.py:248-254` ✅ **已修复**
  - ~~非常长的密码会在终端输出中尴尬换行~~
  - ~~建议：超过一定长度的密码显示时截断并添加省略号~~
  - **修复内容**：
    - 根据终端宽度动态计算最大显示长度（`max(40, terminal_width - 10)`）
    - 超长密码截断并追加 `"..."`
    - 保持索引对齐，避免破坏表格格式

---

## 9. 现代 Python 实践

- [x] **MEDIUM-18**: 可使用 `typing.Protocol` 定义 PasswordStore - `try7z/password_manager.py:58-281` ✅ **已修复**
  - ~~密码存储接口未使用结构子类型~~
  - **修复内容**：
    - 添加 `PasswordStore` Protocol（`@runtime_checkable`），定义 `add_password`、`remove_password`、`get_passwords`、`clear_passwords`、`count`、`remove_by_index` 方法
    - `PasswordManager` 隐式实现该协议，获得结构子类型检查支持
    - 新增 doctest 示例展示 Protocol 用法

- [x] **MEDIUM-19**: 包 `__init__.py` 缺少 `__all__` - `try7z/__init__.py` ✅ **已修复**
  - ~~无 `__all__` 定义来控制公共 API 导出~~
  - **修复内容**：
    - 添加 `__all__ = ["__version__", "__build_date__"]` 控制公共 API 导出

- [x] **LOW-10**: 可更一致地使用 `pathlib` - `try7z/utils.py:160-163` ✅ **已修复**
  - ~~使用 `os.environ.get()` 而非 `Path` 处理环境变量~~
  - **修复内容**：
    - 使用海象运算符 `:=` 重构环境变量获取，使赋值和条件判断更紧凑，与 `pathlib` 风格更一致

- [x] **LOW-11**: `get_supported_extensions()` 每次调用创建新 set - `try7z/utils.py:230-248` ✅ **已修复**
  - ~~函数每次调用都创建新 set，而它是常量~~
  - **修复内容**：
    - 模块级别定义 `SUPPORTED_EXTENSIONS: frozenset[str] = frozenset({".7z", ".zip", ".rar"})`
    - `is_supported_archive()` 直接使用 `SUPPORTED_EXTENSIONS`
    - `get_supported_extensions()` 返回 `set(SUPPORTED_EXTENSIONS)` 保持向后兼容
    - 新增 `TestSupportedExtensions::test_supported_extensions_constant` 测试

---

## 10. 逐行问题汇总

| 文件 | 行号 | 问题 | 优先级 |
|------|------|------|--------|
| `try7z/extractor.py` | 116 | 裸 `except Exception` | High |
| `try7z/extractor.py` | 320 | 内联 `import shutil` | Low |
| `try7z/extractor.py` | 406 | 过于宽泛的密码错误检测 | Medium |
| `try7z/extractor.py` | 440 | 内联 `from tqdm import tqdm` | Low |
| `try7z/extractor.py` | 467-513 | 逐字节 stdout 读取 | Medium |
| `try7z/main.py` | 106 | 反直觉退出码 | Medium |
| `try7z/main.py` | 166-171 | 手动去重而非 `dict.fromkeys` | Low |
| `try7z/main.py` | 292-302 | 确认提示后创建管理器 | Medium |
| `try7z/main.py` | 378 | 裸 `except Exception` | High |
| `try7z/main.py` | 439-453 | 目录删除竞态条件 | High |
| `try7z/password_manager.py` | 118-119 | 损坏 JSON 时静默数据丢失 | Critical |
| `try7z/password_manager.py` | 154 | 无密码内容验证 | High | ✅ 已修复 |
| `try7z/password_manager.py` | 276-277 | 错误消息误导（0-based vs 1-based） | High |
| `try7z/utils.py` | 160-163 | 关于目录创建的误导文档字符串 | Medium |

---

## 推荐修复优先级顺序

- [x] 1. **CRITICAL**: 修复 `_load_passwords()` 中的静默数据丢失 ✅
- [x] 2. **HIGH**: 添加密码输入验证 ✅
- [ ] 3. **HIGH**: 修复 `cmd_extract()` 输出目录处理的竞态条件
- [ ] 4. **HIGH**: 重构 `try_extract()` 避免双重提取
- [ ] 5. **HIGH**: 改进错误处理特异性
- [x] 6. **HIGH**: 降低 `cmd_remove_password()` 和 `cmd_extract()` 复杂度 ✅（已完成策略模式重构）
- [x] 7. **MEDIUM**: 添加缺失的测试覆盖 ✅（核心测试已全部补全）
- [ ] 8. **MEDIUM**: 改进密码存储安全性
- [ ] 9. **MEDIUM**: 修复进度条读取性能问题
- [x] 10. **LOW**: 文档、代码风格及用户体验改进 ✅

---

*报告结束 - 共 47 项优化建议，34 项已修复，13 项待处理*
